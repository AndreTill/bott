import type { TelegramObjects } from "@gramio/types";
import { User } from "../user";
import { ChatBoostSource } from "./chat-boost-source";
/** The boost was obtained by subscribing to Telegram Premium or by gifting a Telegram Premium subscription to another user. */
export declare class ChatBoostSourcePremium extends ChatBoostSource {
    payload: TelegramObjects.TelegramChatBoostSourcePremium;
    constructor(payload: TelegramObjects.TelegramChatBoostSourcePremium);
    /** Source of the boost, always `premium` */
    get source(): "premium";
    /** User that boosted the chat */
    get user(): User;
}
