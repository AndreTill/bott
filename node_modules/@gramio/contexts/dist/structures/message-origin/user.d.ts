import type { TelegramObjects } from "@gramio/types";
import { User } from "../user";
import { MessageOrigin } from "./message-origin";
/** The message was originally sent by a known user. */
export declare class MessageOriginUser extends MessageOrigin {
    payload: TelegramObjects.TelegramMessageOriginUser;
    constructor(payload: TelegramObjects.TelegramMessageOriginUser);
    /** Type of the message origin, always `user` */
    get type(): "user";
    /** Date the message was sent originally in Unix time */
    get date(): number;
    /** User that sent the message originally */
    get senderUser(): User;
}
